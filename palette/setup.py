import setuptools

setuptools.setup(
    name="palette",
    version="0.0.1",
    description="A set of model and function to support image restoration",
    packages=setuptools.find_packages(),
)